## Understanding the Data

-- This data has 11 Column where 4 of them are INTEGER, 3 of them are FLOAT, 2 of them are VARCHAR(15), 1 of them is DATE and 1 is TIME DataTypes.
-- Total Number of Row is 2000 where three rows were deleted to eliminate the null values.
